# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GithubConnect' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GithubConnect

pod 'Auth0', '~> 1.0'

	
  target 'GithubConnectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GithubConnectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
